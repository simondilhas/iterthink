"""LLM routing, Ollama helpers, embeddings, and OS keyring passphrase storage.

Import submodules explicitly (e.g. ``from iterthink.ai import ollama_util``) to
avoid pulling optional heavy dependencies at package import time.
"""
