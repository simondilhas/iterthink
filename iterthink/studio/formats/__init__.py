"""Non-Markdown compare renderers (PDF, Word, IFC, …).

Each submodule is a ``MarkdownStudio*`` mixin; the orchestrator in
``iterthink.studio.history`` dispatches via ``_rebuild_compare_view()``.
"""
